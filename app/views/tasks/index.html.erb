<header>
  <h1>タスク一覧</h1>
</header>
<table >
  <tr >
    <th>タスク名</th>
    <th>内容</th>
  </tr>
  <% @task.each do |task| %>
  <tr >
    <td><%= task.name %></td>
    <td><%= task.content %></td>
    <td><%= link_to '詳細を確認する', task_path(task) %></td>
    <td><%= link_to "タスクを編集する", edit_task_path(task) %></td>
    <td><%= link_to 'タスクを削除する', task_path(task), method: :delete %></td>
  </tr>
  <% end %>
</table>
<%= link_to '新しいタスクを追加', new_task_path %>
